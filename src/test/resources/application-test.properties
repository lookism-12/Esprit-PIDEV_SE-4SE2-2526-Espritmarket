# Test configuration - used when running tests
# Use test MongoDB or embedded MongoDB if needed

server.port=0

# MongoDB for tests (adjust if using Testcontainers or embedded)
spring.data.mongodb.uri=mongodb://localhost:27017/espritmarket_test

# JWT - test secret (min 32 chars for HS256)
jwt.secret=EspritMarketTestSecretKeyMin32BytesForTests!
jwt.expirationMs=3600000
